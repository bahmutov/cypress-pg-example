/// <reference path="./index.d.ts" />

Cypress.Commands.add('query', (query, params) => {
  // call cy.task('query') passing the parameters
  // make Cypress.log object to provide diagnostic
  // information when we need to debug the command
})
